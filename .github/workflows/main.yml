name: Learning github actions

on: 
  create

jobs:
  callme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
      - run: yarn
      
      
